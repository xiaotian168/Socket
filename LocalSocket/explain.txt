本地套接字：
    1.用于系统内部通信，不能用于网络通信的套接字
    2.意义：
        将网络通信和本地进程间通信接口统一

    3.如果TCP、UDP套接字通信的服务器IP设置成本地换回接口IP:127.0.0.1；则相当于本地套接字
    4.跟命名管道差不多




注意：
    每次服务器退出后；需要删掉通信的文件才能重新执行通过
    rm sock_file



关于errno错误码
    1.包含在头文件errno.h中的全局变量
    2.值为Linux C api函数最后一次执行错误的错误码
